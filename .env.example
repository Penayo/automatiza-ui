# Base URL of the bpmn-engine API (no trailing slash)
VITE_API_HOST=http://localhost:3000

# Key used to encrypt access info stored in localStorage — generate with: openssl rand -hex 32
VITE_CRYPTO_KEY=REPLACE_WITH_STRONG_RANDOM_KEY
